export class OutlineTransformer {
  transformMarkdown(markdown: string): string {
    return markdown;
  }

  transformImageUrl(originalUrl: string, newUrl: string): string {
    return newUrl;
  }
}

export class OutlineTransformer {
    transformMarkdown(markdown) {
        return markdown;
    }
    transformImageUrl(originalUrl, newUrl) {
        return newUrl;
    }
}
//# sourceMappingURL=transformer.js.map